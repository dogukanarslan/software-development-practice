<%- include('partials/header'); %>

<h4>Your Pins</h4>

<% if (pins.length > 0) { %>
<%- include('partials/table', {data: {header: ['Title', 'Description', 'Creator', 'Link'], body:  pins}}); %>
<% } else { %>
    <p>You don't have any pin yet. <a href="/pins/create">Create a new pin</a></p>
<% } %>

<%- include('partials/footer'); %>
