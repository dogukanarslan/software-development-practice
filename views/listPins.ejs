<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head') %>
  <body>
    <%- include('partials/navbar') %>
    <div class="container">
      <h4>Your Pins</h4>
      <% if (pins.length > 0) { %> <%- include('partials/table', {data: {header:
      ['Title', 'Description', 'Creator', 'Total Likes', 'Total Dislikes',
      'Link'], body: pins}}); %> <% } else { %>
      <p>
        You don't have any pin yet. <a href="/pins/create">Create a new pin</a>
      </p>
    </div>
    <% } %> <%- include('partials/footer'); %>
  </body>
</html>
